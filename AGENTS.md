# Housemaker 開発ルール

3D ハウスモデリング Web アプリを開発する。React 19、TypeScript、SVG、Zustand、Three.js、React Three Fiber 9 を使用する。ビルドと配信はリポジトリの Vite / Vinext 設定を確認する。

## コードコメント

- 記述する各コードファイルには、日本語のロジック解説コメントを必ず含める。
- ファイル冒頭で責務を説明し、主要関数と分岐では計算の目的、単位、座標変換、入力制約、状態変更や副作用の理由を説明する。
- 自明な構文の逐語説明ではなく、保守時に必要な設計意図を書く。既存の説明は実装変更に合わせて更新する。
- JSON などコメントを許さない形式にはコメント構文を挿入せず、対応する README または設定説明に意図を書く。

## サブエージェント構成

独立した実装、レビュー、テスト作業は以下の担当へ委譲する。親スレッドが通常のオーケストレーターとなる。

| 役割 | 責任 | 書込境界 |
| --- | --- | --- |
| orchestrator | 仕様、共有モデル、作業分担、統合、最終検証 | 共通型、Zustand ストア、依存関係、設定、全体 CSS |
| ui_engineer | SVG 間取りと編集 UI、家具カタログ、プロパティ | 親が割り当てた UI ファイルのみ |
| scene_engineer | Three.js / R3F の部屋、壁、家具とカメラ | 親が割り当てた 3D ファイルのみ |
| code_reviewer | 状態、座標、履歴、操作、コード品質の確認 | 読み取り専用 |
| test_engineer | 状態テストとブラウザでの一連の操作検証 | 親が割り当てたテストと証跡のみ |

- 委譲時に具体的な対象ファイル、入出力契約、期待する結果を伝える。同一ファイルの同時書込は禁止する。
- 所有者以外のファイルを直す必要が生じたら、親へ変更内容を報告する。親が順序を決める。
- 親を含めた環境の同時実行制限を守り、枠が足りない場合は段階的に担当を起動する。
- モデルと推論設定を役割に固定しない。生成時の明示指定と共通の既定値も設けず、親の選択を継承する。
- レビューとテストの結果を回収して必要な修正と再確認を終えてから、親が完了を報告する。

## データと操作の設計

- 部屋、壁、家具は安定した ID で扱い、Zustand の単一状態を 2D / 3D から参照する。
- 長さの単位、角度の単位、SVG 平面と Three.js の X/Z 平面の対応を共有型と変換関数に明記する。
- 一時的なドラッグ状態と確定したドキュメントを分け、1 操作を 1 回の Undo にまとめる。
- 寸法や座標の入力を検証し、NaN、無限値、負の寸法が保存データや描画へ流れないようにする。
- 家具の配置・移動・回転は共有状態を更新し、両ビューに反映する。選択対象が削除された場合は選択を解除する。
- ブラウザの永続化や WebGL が利用できない場合も、原因を説明し基本 UI を維持する。

## 検証と運用

- リポジトリの型検査・ビルドと、変更に必要なテストを実行する。
- 部屋作成、寸法編集、壁追加、家具の配置・移動・回転、2D/3D の同期、Undo/Redo、保存と再読込を確認する。
- キーボード操作、フォーム入力、ドラッグ中断、表示幅変更、コンソールエラーを確認する。
- 確認済みの結果と未実行の範囲を区別する。他の担当の未コミット変更を破棄しない。
- MCP サーバーはユーザーが指定する接続先・用途を確認してから設定する。秘密情報はコードやコミットに含めない。
